---
version: '2'
services:
    rancheros-ansible:
        image: rancheros-ansible 
        build: .
        privileged: true
        command: build /opt/ansible-template/rancheros.json
        volumes: 
            - build:/build
            - /dev/kvm:/dev/kvm:rw
            - /dev/net/tun:/dev/net/tun:rw

volumes:
  build:
